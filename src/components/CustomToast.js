const CustomToast = ({ wallet }) => {
    return (
        <div>
            {wallet} Wallet generated successfully
            <br />
            Additional information can go here.
        </div>
    );
};

export default CustomToast; 